# Server Port & Environment
PORT=4000
NODE_ENV=development

# PostgreSQL Connection String (Port 5434 maps to Docker PostgreSQL 16 container)
DATABASE_URL="postgresql://mobiops_admin:mobiops_secure_pass_2026@localhost:5434/mobiops_camau?schema=public"

# JWT Configuration for Admin RBAC
JWT_SECRET="mobiops_development_jwt_secret_key_camau_2026"
JWT_EXPIRES_IN="7d"

# Allowed CORS Origins (comma-separated list)
CORS_ORIGINS="http://localhost:3000,http://localhost:3001"
